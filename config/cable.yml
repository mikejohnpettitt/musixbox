development:
  adapter: solid_cable
  silence_polling: true

test:
  adapter: test

production:
  # adapter: redis
  # url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  adapter: solid_cable
  # channel_prefix: musixbox_production
